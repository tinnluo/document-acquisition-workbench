# Follow-Up Policy

Follow-up extraction is allowed only when explicitly enabled and when higher-priority source paths have not already produced an approval-grade candidate.

This keeps follow-up search explainable:

- it is a controlled expansion stage
- it is not a default autonomous loop
- it is always traceable in the emitted run artifacts
